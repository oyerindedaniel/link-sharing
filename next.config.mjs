/** @type {import('next').NextConfig} */

// import path from "path";

const nextConfig = {
  images: {
    formats: ["image/avif", "image/webp"],
  },
  //   sassOptions: {
  //     includePaths: [path.join(__dirname, "styles/globals.scss")],
  //   },
};

export default nextConfig;
